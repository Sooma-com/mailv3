# Generated by Ansible
# ==========================================================================
# service type  private unpriv  chroot  wakeup  maxproc command + args
#               (yes)   (yes)   (yes)   (never) (100)
# ==========================================================================
smtp      inet  n       -       n       -       100     smtpd
pickup    unix  n       -       n       60      1       pickup
cleanup   unix  n       -       n       -       0       cleanup
qmgr      unix  n       -       n       300     1       qmgr
tlsmgr    unix  -       -       n       1000?   1       tlsmgr
rewrite   unix  -       -       n       -       -       trivial-rewrite
bounce    unix  -       -       n       -       0       bounce
defer     unix  -       -       n       -       0       bounce
trace     unix  -       -       n       -       0       bounce
verify    unix  -       -       n       -       1       verify
flush     unix  n       -       n       1000?   0       flush
proxymap  unix  -       -       n       -       -       proxymap
proxywrite unix -       -       n       -       1       proxymap
smtp      unix  -       -       n       -       -       smtp
        -o myhostname=c.mx.smail.eu
relay     unix  -       -       n       -       -       smtp
showq     unix  n       -       n       -       -       showq
error     unix  -       -       n       -       -       error
retry     unix  -       -       n       -       -       error
discard   unix  -       -       n       -       -       discard
local     unix  -       n       n       -       -       local
virtual   unix  -       n       n       -       -       virtual
lmtp      unix  -       -       n       -       -       lmtp
anvil     unix  -       -       n       -       1       anvil
scache    unix  -       -       n       -       1       scache
#10026     inet  n       -       n       -       50     smtpd
#        -o smtpd_proxy_filter=
#        -o smtpd_milters=
#        -o smtpd_client_connection_count_limit=1000
#        -o smtpd_authorized_xforward_hosts=127.0.0.0/8,10.0.0.0/8
#        -o smtpd_client_restrictions=
#        -o smtpd_helo_restrictions=
#        -o smtpd_sender_restrictions=
#        -o smtpd_relay_restrictions=
#        -o smtpd_recipient_restrictions=permit_mynetworks,reject
#        -o smtpd_data_restrictions=
#        -o mynetworks=127.0.0.0/8,10.0.0.0/8,195.170.168.0/24
#        -o receive_override_options=no_unknown_recipient_checks
#        -o smtpd_upstream_proxy_protocol=
remax_iworkremax    unix  -       n       n       -       10       pipe
  flags=Rq user=daemon null_sender=
  argv=/etc/postfix-profissional-mx/content-filter/remax_iworkremax.sh -f ${sender} -- ${recipient}
